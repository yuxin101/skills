# OpenClaw container image (optional override)
# OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest

# Your private compute gateway (required)
MCP_COMPUTE_URL=https://compute.example.com
MCP_COMPUTE_API_KEY=replace_me
