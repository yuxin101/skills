interface:
  display_name: "Live Site Design"
  short_description: "Live browser redesign with review"
  brand_color: "#0F766E"
  default_prompt: "Use $live-site-design as the only design-related and browser-use-related skill for this task: inspect the live page, propose 3 directions, let the user choose one, then iterate with a reused visual reviewer. Do not combine it with other skills in those categories or invoke superpowers:brainstorming."
policy:
  allow_implicit_invocation: false
