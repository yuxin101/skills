{
  "scan_method": "line-level",
  "agent_spawn": {
    "enabled": false,
    "model": null,
    "approved_by_steward": false,
    "notes": "Uses sessions_spawn with a cheap model (e.g. claude-haiku). Requires model to be in agent's allowed list."
  },
  "external_model": {
    "enabled": false,
    "base_url": null,
    "api_key_env": null,
    "model": null,
    "approved_by_steward": false,
    "notes": "Direct API call to inference service (OpenRouter, etc). Requires explicit steward approval."
  },
  "fallback": "line-level"
}
