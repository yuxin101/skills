// 病历生成器 - 核心逻辑文件
// 这个文件定义了病历生成的三步流程

const 病历生成器 = {
  // 第一步：收集关键变量
  收集变量: function() {
    console.log("📋 开始收集病历关键信息...");
    console.log("请提供以下信息：");
    console.log("1. 主诉及相关情况（主要症状、持续时间、诱因等）");
    console.log("2. 性别（男性/女性）");
    console.log("3. 年龄");
    console.log("4. 其他可能信息（婚姻状况、职业等，可选）");
    
    // 这里会收集用户输入
    return {
      主诉: "",
      性别: "",
      年龄: "",
      其他信息: {}
    };
  },

  // 第二步：询问补充
  询问补充: function(收集到的变量) {
    console.log("📝 已收集的信息：");
    console.log(`主诉：${收集到的变量.主诉}`);
    console.log(`性别：${收集到的变量.性别}`);
    console.log(`年龄：${收集到的变量.年龄}`);
    console.log("其他信息：", 收集到的变量.其他信息);
    console.log("\n请确认以上信息是否正确，或者需要补充什么？");
  },

  // 第三步：生成病历
  生成病历: function(最终变量) {
    console.log("🔄 正在生成规范的入院记录...");
    
    // 这里会调用专业的病历生成逻辑
    const 病历 = 生成规范病历(最终变量);
    return 病历;
  }
};

// 专业的病历生成函数
function 生成规范病历(变量) {
  // 这里实现完整的病历生成逻辑
  // 按照用户提供的prompt模板生成
  return "规范的入院记录...";
}

module.exports = 病历生成器;