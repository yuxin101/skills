[project]
name = "talking-video-auto-edit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
# 主依赖使用 == 锁定（不依赖 uv.lock，便于仅支持传统清单的托管环境）
dependencies = [
    "requests==2.32.5",
    "python-dotenv==1.2.2",
    "volcengine==1.0.218",
]

[tool.setuptools]
py-modules = ["api_manage", "volc_request", "asr_volc", "pipeline_url_to_asr", "main"]
