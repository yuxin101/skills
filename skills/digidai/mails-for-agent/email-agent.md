# Deprecated

This file has been merged into [SKILL.md](./SKILL.md).

Use `SKILL.md` instead -- it follows the OpenClaw AgentSkills format
(YAML frontmatter + env var configuration) and contains the complete
API reference.

If you were previously using this file as a standalone system prompt,
use `skills/universal/email-api.md` instead.
