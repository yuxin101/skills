# 飞书配置助手使用示例 (Python 版)

## 安装

```bash
pip install -r requirements.txt
openclaw skill install ./
```

## 使用

- `/config` - 打开配置菜单
- `/feishu status` - 查看状态
- `/feishu doctor` - 运行诊断
- `/feishu help` - 帮助

## 配置项

支持流式输出、多任务并行、群聊模式、卡片样式等配置。
