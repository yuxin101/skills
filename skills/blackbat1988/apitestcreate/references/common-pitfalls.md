# 接口测试100个高频易错点清单

> 涵盖请求规范、参数处理、数据校验、业务逻辑、性能安全、兼容适配六大核心维度

---

## 一、请求层基础规范（1-15）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 1 | 请求方法错误（GET用成POST、DELETE用成PUT，或非幂等接口用GET） | 使用错误的HTTP方法调用接口 | 返回405 Method Not Allowed或明确错误提示 |
| 2 | 请求头缺失必选字段（如Token、Content-Type、Accept） | 删除必选请求头后调用 | 返回401或明确提示缺少必选头 |
| 3 | Content-Type与实际传参格式不匹配 | json传参却设为form-data | 返回400参数格式错误 |
| 4 | 请求头字段值格式错误 | Token拼接多余空格、时间戳单位不统一 | 返回401或格式错误提示 |
| 5 | 接口地址拼写错误 | 修改URL路径字符 | 返回404 Not Found |
| 6 | 跨域请求未携带CORS相关头 | 跨域调用不传Origin头 | 返回CORS错误或正确处理跨域 |
| 7 | 请求协议错误 | 生产环境用HTTP调用HTTPS接口 | 返回协议错误或自动重定向 |
| 8 | 请求超时时间未设置/设置不当 | 设置极短超时（如1ms） | 返回超时错误，不无限等待 |
| 9 | 批量请求未做连接复用 | 高频调用观察TCP连接数 | 应复用连接，无频繁创建 |
| 10 | 请求体编码格式错误 | 中文用非UTF-8编码 | 正确处理或返回编码错误 |
| 11 | GET请求传参过长 | URL超过2048字符 | 返回414 URI Too Long |
| 12 | POST请求体为空但要求必传 | 发送空请求体 | 返回参数缺失错误 |
| 13 | 请求重定向未处理 | 接口返回301/302 | 正确跟随重定向或返回提示 |
| 14 | 多环境地址混淆 | 测试环境用生产地址 | 环境隔离，返回错误或提示 |
| 15 | 接口版本号未传/传错 | 传错误的version参数 | 返回版本错误或调用正确版本 |

---

## 二、参数处理与校验（16-45）

### （一）必选/可选参数（16-25）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 16 | 必选参数完全缺失，接口未返回明确错误提示 | 删除必选参数 | 返回明确的参数缺失提示 |
| 17 | 必选参数传空值（""、null、undefined） | 传空字符串/null | 返回参数无效错误 |
| 18 | 可选参数传非法值时，接口未做默认值兜底 | 可选参数传非法值 | 使用默认值或忽略 |
| 19 | 多参数组合时，部分必选参数缺失 | 传部分必选参数 | 返回缺失参数提示 |
| 20 | 参数名大小写错误 | userID写成userid | 返回参数错误或兼容处理 |
| 21 | 参数名拼写错误 | mobile写成moblie | 返回参数错误 |
| 22 | 重复传参 | 同一参数在URL和Body中同时传 | 明确取值规则，无歧义 |
| 23 | 多余参数传参 | 传未定义的参数 | 忽略或返回警告 |
| 24 | 动态参数传固定值 | /api/{id}传非占位符值 | 正确解析或返回错误 |
| 25 | 批量参数中部分必选参数缺失 | 批量请求中某条缺失必选项 | 返回具体哪条缺失 |

### （二）参数格式与类型（26-35）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 26 | 字符串参数传数值类型 | name传123 | 类型转换或返回类型错误 |
| 27 | 数值参数传字符串/特殊字符 | age传"二十"、"12a" | 返回类型格式错误 |
| 28 | 数值参数超出取值范围 | 年龄传-1、200，金额传负数 | 返回范围错误 |
| 29 | 浮点型参数精度丢失 | 金额传0.019999 | 存储和返回精度正确 |
| 30 | 布尔参数传非true/false值 | 传1/0、"true"/"false" | 类型转换或返回错误 |
| 31 | 日期参数格式错误 | yyyy-mm-dd写成yyyy/mm/dd | 返回日期格式错误 |
| 32 | 数组参数传非数组格式 | 传"1,2,3"而非[1,2,3] | 返回格式错误 |
| 33 | 数组参数为空数组（[]） | 传空数组[] | 正确处理，不报错 |
| 34 | 对象参数传非对象格式 | 传字符串而非{} | 返回格式错误 |
| 35 | 枚举参数传非枚举值 | 性别传3，枚举仅0/1 | 返回枚举值错误 |

### （三）参数长度与边界（36-45）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 36 | 字符串参数长度为0 | 传空字符串"" | 返回最小长度错误 |
| 37 | 字符串参数超过最大长度限制 | 用户名限20字符，传21字符 | 返回超长错误 |
| 38 | 数组参数元素数量超限 | 批量操作限50条，传51条 | 返回数量超限错误 |
| 39 | 数值参数为0，接口未做零值校验 | 分页size传0 | 返回参数错误或使用默认值 |
| 40 | 数值参数为最大/最小临界值 | int型传2147483647 | 正确处理，不溢出 |
| 41 | 固定长度参数长度不符 | 手机号传10位/12位 | 返回长度错误 |
| 42 | 密码未满足复杂度 | 纯数字密码 | 返回复杂度不足错误 |
| 43 | 文本参数包含特殊字符 | 传<、>、&、'、" | 正确转义处理 |
| 44 | 分页参数传负数 | pageNum=-1 | 返回参数错误 |
| 45 | 多条件查询参数边界组合 | 极端参数组合 | 结果正确或友好提示 |

---

## 三、响应结果校验（46-65）

### （一）响应基础信息（46-55）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 46 | 状态码返回错误 | 成功操作返回500 | 状态码正确反映结果 |
| 47 | 状态码与业务码不匹配 | 200状态码+失败业务码 | 状态码与业务码一致 |
| 48 | 响应头缺失必选字段 | 检查Set-Cookie等 | 必选响应头存在 |
| 49 | 响应体编码格式错误 | 检查响应编码 | UTF-8编码，无乱码 |
| 50 | 响应超时 | 长时间操作 | 在约定时间返回或超时提示 |
| 51 | 响应体为空但要求返回数据 | 查询操作 | 返回空数组而非空响应体 |
| 52 | 响应体多余字段 | 对比接口文档 | 仅返回文档定义字段 |
| 53 | 响应体缺失必选字段 | 检查必返字段 | 所有必返字段存在 |
| 54 | 响应字段名与文档不一致 | 对比字段名 | 字段名与文档一致 |
| 55 | 响应字段值类型与文档不符 | 检查字段类型 | 类型与文档定义一致 |

### （二）业务数据与逻辑（56-65）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 56 | 业务码返回错误 | 成功操作检查code | 业务码正确 |
| 57 | 错误提示信息不明确 | 触发错误场景 | 返回具体错误原因 |
| 58 | 相同请求多次返回结果不一致 | 相同参数调用多次 | 静态数据结果一致 |
| 59 | 动态数据返回不实时 | 新增后立即查询 | 返回最新数据 |
| 60 | 分页总条数计算错误 | 验证total字段 | 总条数计算正确 |
| 61 | 排序结果未按指定字段排序 | 传排序参数 | 结果按指定字段排序 |
| 62 | 过滤结果包含不符合条件数据 | 传过滤条件 | 结果全部符合条件 |
| 63 | 批量操作结果与实际不符 | 批量操作5条 | 返回结果与实际一致 |
| 64 | 空结果集返回格式错误 | 查询无结果 | 返回[]而非null |
| 65 | 响应数据精度丢失 | 查询金额0.01 | 返回精确值 |

---

## 四、业务逻辑与流程（66-80）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 66 | 接口未做幂等性处理 | 重复提交订单 | 仅创建一条订单 |
| 67 | 幂等性标识校验失效 | 相同requestId重复请求 | 拒绝重复请求 |
| 68 | 接口权限校验缺失 | 无Token访问 | 返回401 Unauthorized |
| 69 | 越权访问 | A用户访问B用户数据 | 返回403 Forbidden |
| 70 | 数据关联逻辑错误 | 创建订单不传用户ID | 返回参数错误 |
| 71 | 状态流转错误 | 未付款订单直接发货 | 返回状态错误 |
| 72 | 关联表未同步更新 | 删除用户后查订单 | 订单已标记作废 |
| 73 | 条件分支逻辑遗漏 | 传未处理的参数组合 | 有明确处理或错误提示 |
| 74 | 异常场景未做兜底 | 查询不存在的ID | 返回友好提示，不抛异常 |
| 75 | 批量操作全成功/全失败 | 批量操作中部分失败 | 返回部分成功详情 |
| 76 | 前置条件未满足仍执行 | 未登录提交订单 | 返回未登录错误 |
| 77 | 前置接口失败后续仍调用 | 流程中断 | 后续接口不执行 |
| 78 | 数据唯一性校验失效 | 重复注册手机号 | 返回已注册错误 |
| 79 | 数据有效期校验失效 | 使用过期验证码 | 返回验证码过期 |
| 80 | 业务规则冲突 | 优惠券叠加超限 | 返回超限错误 |

---

## 五、性能与并发（81-90）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 81 | 高并发请求服务器崩溃 | 1000 QPS压测 | 服务稳定，无大量5xx |
| 82 | 并发导致数据脏读/脏写 | 多用户同时修改同一数据 | 数据一致性正确 |
| 83 | 接口响应时间过长 | 单接口性能测试 | P99 < 约定阈值 |
| 84 | 服务器资源耗尽 | 高负载测试 | 资源占用合理 |
| 85 | 接口未做限流处理 | 突发大流量 | 触发限流，服务稳定 |
| 86 | 大页码查询响应慢 | pageNum=10000 | 响应时间合理 |
| 87 | 批量操作未分批处理 | 传10000条数据 | 返回数量限制错误 |
| 88 | 第三方服务无超时控制 | 模拟第三方超时 | 本接口有超时兜底 |
| 89 | 高频查询未做缓存 | 重复查询相同数据 | 命中缓存，响应快 |
| 90 | 缓存与数据库不一致 | 修改数据后查询 | 返回最新数据 |

---

## 六、安全与兼容（91-100）

### （一）安全漏洞（91-95）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 91 | SQL注入漏洞 | id=1 or 1=1 | 返回参数错误，不返回全量数据 |
| 92 | XSS漏洞 | name=<script>alert(1)</script> | 转义处理，不执行脚本 |
| 93 | CSRF漏洞 | 跨站不带Token请求 | 拒绝请求 |
| 94 | 敏感数据明文传输 | 查看密码/身份证字段 | 数据已脱敏或加密 |
| 95 | 接口未做频率限制 | 暴力调用验证码接口 | 触发频率限制 |

### （二）兼容与异常（96-100）

| 序号 | 易错点 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 96 | 不同客户端结果不一致 | Chrome/IE/移动端分别调用 | 结果一致 |
| 97 | 不同环境行为不一致 | 测试/生产环境对比 | 行为一致 |
| 98 | 超大请求体导致崩溃 | 传100MB请求体 | 返回大小限制错误 |
| 99 | 网络中断未处理 | 模拟网络中断 | 有重试/降级机制 |
| 100 | 第三方服务挂掉未熔断 | 模拟第三方不可用 | 触发熔断，本服务可用 |

---

## 七、补充测试项（用户补充）

### （一）通用信息校验

| 序号 | 测试项 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 101 | URL正确性 | 填写正确的URL | 正确访问接口 |
| 102 | URL错误 | 填写错误的URL | 访问不到，返回404 Not Found |
| 103 | 请求方法正确 | 使用正确的HTTP方法 | 请求成功 |
| 104 | 请求方法错误 | 使用错误的HTTP方法（如GET代替POST） | 返回405 Method Not Allowed |
| 105 | 请求头正确 | 正确传递Content-Type: application/json | 请求成功处理 |
| 106 | 请求头错误 | Content-Type与实际格式不匹配 | 返回400参数格式错误 |
| 107 | 接口鉴权正确 | 传递正确的token/session | 请求成功 |
| 108 | 接口鉴权缺失 | 不传认证信息 | 返回401 Unauthorized |
| 109 | 接口鉴权错误 | 传递错误的token/session | 返回401认证失败 |
| 110 | 认证信息失效 | 使用过期或失效的认证信息 | 返回401或重新登录提示 |

### （二）接口参数详细校验

| 序号 | 测试项 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 111 | 必填项完整 | 传递所有必填项，值合法 | 请求成功 |
| 112 | 必填项不传 | 删除某个必填参数 | 返回明确的参数缺失提示 |
| 113 | 必填项为空值 | 参数传None/null | 返回参数无效错误 |
| 114 | 必填项为空字符串 | 参数传"" | 返回参数不能为空错误 |
| 115 | 选填项都不填 | 所有选填项都不传 | 请求成功，使用默认值 |
| 116 | 选填项部分传递 | 只传递部分选填项 | 请求成功，未传的使用默认值 |
| 117 | 参数长度超上限 | 参数值超过maxLength | 返回参数长度超限错误 |
| 118 | 参数长度低于下限 | 参数值小于minLength | 返回参数长度不足错误 |
| 119 | 参数长度等于上限 | 参数值等于maxLength | 请求成功 |
| 120 | 参数类型正确 | 传递正确的数据类型 | 请求成功 |
| 121 | 参数类型错误 | 传递错误的数据类型（如字符串传整数） | 返回类型错误 |
| 122 | 参数有效性范围内 | 传递有效范围内的值 | 请求成功 |
| 123 | 参数有效性范围外 | 传递超出范围的值（如负数金额） | 返回参数无效错误 |
| 124 | 唯一字段数据唯一 | 唯一字段传不同值 | 请求成功 |
| 125 | 唯一字段数据重复 | 唯一字段传重复值 | 返回重复错误 |
| 126 | 非唯一字段可重复 | 非唯一字段传重复值 | 请求成功 |
| 127 | 参数关联校验 | 同时传互斥参数 | 返回参数关联错误提示 |
| 128 | 参数依赖校验 | 依赖参数缺失 | 返回依赖参数缺失错误 |

### （三）其他补充项

| 序号 | 测试项 | 测试方法 | 预期结果 |
|------|--------|----------|----------|
| 129 | 幂等性-重复提交 | 多次提交相同请求 | 效果与一次提交相同 |
| 130 | 幂等性-抽奖场景 | 多次提交抽奖请求 | 只能成功一次 |
| 131 | 幂等性-修改订单 | 多次提交修改请求 | 数据最终一致，无重复变更 |
| 132 | 弱网环境-网络中断 | 提交过程中网络中断 | 事务回滚，无脏数据 |
| 133 | 弱网环境-支付场景 | 支付过程网络中断 | 不扣款或自动退款 |
| 134 | 分布式-数据同步 | nginx配置模拟分布式 | 数据正确同步 |
| 135 | 分布式-一致性 | 多节点同时操作 | 数据一致性保证 |
| 136 | 接口风格-RESTful | 检查URL命名、HTTP方法使用 | 符合RESTful规范 |
| 137 | 接口风格-规范性 | 检查状态码使用、资源命名 | 符合规范 |
| 138 | 敏感信息-传输加密 | 抓包检查传输数据 | 敏感信息已加密 |
| 139 | 敏感信息-日志脱敏 | 检查日志输出 | 敏感信息已脱敏 |
| 140 | 敏感信息-存储加密 | 检查数据库存储 | 敏感信息已加密存储 |

---

## 快速自检清单

### 接口测试必检项（Top 20）

```
□ 1. 必选参数缺失校验
□ 2. 参数类型/格式校验
□ 3. 参数边界值校验（最小、最大、空值）
□ 4. 状态码与业务码一致性
□ 5. 响应字段完整性
□ 6. 权限校验（未登录、越权）
□ 7. 幂等性校验
□ 8. SQL注入测试
□ 9. XSS攻击测试
□ 10. 数据唯一性校验
□ 11. 状态流转校验
□ 12. 分页查询正确性
□ 13. 排序功能正确性
□ 14. 过滤条件正确性
□ 15. 并发数据一致性
□ 16. 响应时间在阈值内
□ 17. 错误提示明确
□ 18. 空结果返回空数组[]
□ 19. 敏感数据脱敏
□ 20. 第三方服务超时兜底
```

---

## 按测试阶段分类

### 功能测试阶段重点（1-80）

| 阶段 | 重点序号 |
|------|----------|
| 冒烟测试 | 1, 16, 26, 46, 56, 68 |
| 参数校验 | 16-45 |
| 业务逻辑 | 66-80 |
| 响应校验 | 46-65 |

### 非功能测试阶段重点（81-100）

| 阶段 | 重点序号 |
|------|----------|
| 性能测试 | 81-90 |
| 安全测试 | 91-95 |
| 兼容测试 | 96-100 |

---

## 自动化测试用例生成建议

根据高频易错点，优先自动生成以下类型测试用例：

| 优先级 | 测试类型 | 覆盖易错点 |
|--------|----------|------------|
| P0 | 必选参数校验 | 16-18, 36-37 |
| P0 | 权限校验 | 68-69 |
| P0 | 幂等性校验 | 66-67 |
| P0 | SQL/XSS注入 | 91-92 |
| P1 | 参数类型校验 | 26-35 |
| P1 | 边界值校验 | 36-45 |
| P1 | 状态码校验 | 46-47, 56 |
| P1 | 响应字段校验 | 51-55 |
| P2 | 性能边界 | 86-87 |
| P2 | 兼容性 | 96-97 |
