# Notes

This skill is now being converted into a fully self-contained ClawHub-installable skill.

Design rule:
- no silent fallback behavior
- fail clearly if the backend or environment is not available
