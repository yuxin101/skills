# TOOLS.md

## Tool Settings

- When testing with browse, always set `CONTAINER=1` in Docker environments
- Default vision model for sub-agents: `openrouter/xiaomi/mimo-v2-omni`
- Use `gh` CLI for GitHub operations (PRs, issues, CI)
- Default branch detection: check `origin/main` then `origin/master`
