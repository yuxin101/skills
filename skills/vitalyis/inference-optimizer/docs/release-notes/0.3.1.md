# Release Notes: v0.3.1

## Summary

ClawHub / OpenClaw registry metadata: `SKILL.md` frontmatter declares install commands (`cliHelp`), on-disk state paths (`config.stateDirs` and `config.example`), repository links, and `requires.bins` (`bash`, `python3`) under `metadata.openclaw` so automated security review can align declared footprint with the skill’s behavior. Exec allowlist examples in `SKILL.md` and `SECURITY.md` use path-specific bash lines per script (no `/usr/bin/bash *` / `**`); NVM examples stay bounded.

## Files updated

- `SKILL.md`
- `SECURITY.md`
- `CHANGELOG.md`
- `social-preview.png`
