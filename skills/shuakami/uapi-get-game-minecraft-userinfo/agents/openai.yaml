interface:
  display_name: "UAPI 查询 MC 玩家 接口"
  short_description: "使用 UAPI 的查询 MC 玩家接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-game-minecraft-userinfo 直接调用 GET /game/minecraft/userinfo，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
