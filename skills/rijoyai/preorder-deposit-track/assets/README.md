# Assets (skill-creator compliant)

This folder is for **optional files used in output**: templates, icons, fonts (per skill-creator anatomy). Currently empty.

- Add files here if the skill produces outputs that reference them (e.g. milestone dashboard template, email copy file).
- Reference assets from SKILL.md with clear paths; use forward slashes (e.g. `assets/milestone_dashboard.md`).
