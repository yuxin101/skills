{
  "id": "q-wms-test",
  "name": "Q-WMS (Test)",
  "description": "Q-WMS 场景路由插件。负责授权流程、版本检查、把工具调用转发给 open-platform-claw 后端。",
  "skills": ["./skills"],
  "managedSkillId": "q-wms-test",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "defaultAuthBaseUrl": {
        "type": "string",
        "description": "open-platform-claw 服务地址，如 http://qlink-portal-test.800best.com"
      },
      "openclawInstanceId": {
        "type": "string",
        "description": "本 OpenClaw 实例标识，用于授权请求溯源，默认 openclaw-q-wms-test"
      },
      "managedSkillId": {
        "type": "string",
        "description": "ClawHub 上发布的 skill slug，用于版本检查，默认 q-wms-test"
      },
      "defaultTimeoutSeconds": {
        "type": "number",
        "minimum": 1,
        "description": "后端请求超时秒数，默认 20"
      },
      "versionCheckTimeoutMs": {
        "type": "number",
        "description": "版本检查超时毫秒数，默认 4000"
      },
      "versionCheckCacheTtlMs": {
        "type": "number",
        "description": "版本检查结果缓存时间毫秒数，默认 60000"
      }
    }
  }
}
