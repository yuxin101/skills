# GUI Agent dependencies
# Install: pip install -r requirements.txt
# Or run: bash scripts/setup.sh (recommended)

torch
torchvision
torchaudio
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
huggingface-hub
transformers
accelerate

# Optional: for Claude/GPT API integration
anthropic
openai
