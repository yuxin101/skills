# 📚 文档更新说明

## ✅ 已完成的改进

### 1. 移除硬编码路径

**之前**:
- ❌ `D:\work\xiaohongshu\xhs-mcp-service` (Windows绝对路径)
- ❌ `cd D:\work\xiaohongshu\xhs-mcp-service`

**现在**:
- ✅ `xhs-mcp-service` (相对路径，名称可自定义)
- ✅ `cd xhs-mcp-service`
- ✅ 使用环境变量配置
- ✅ 提供跨平台示例

### 2. 文档结构优化

```
xhs-mcp-service/           # 你的项目目录（名称可自定义）
├── README.md              ✅ 完整使用说明（已通用化）
├── 快速开始.md            ✅ 快速参考卡片（已通用化）
├── 文档导航.md            ✅ 文档索引
├── examples/
│   ├── 01-基础示例.mjs    ✅ 移除硬编码
│   ├── 02-互动示例.mjs    ✅ 移除硬编码
│   └── 03-发布示例.mjs    ✅ 详细说明相对/绝对路径
└── src/
    └── ...
```

### 3. 路径说明改进

#### 在代码中
```javascript
// ✅ 使用相对路径（推荐）
images: ['./photos/img1.jpg', './photos/img2.jpg']

// ✅ 使用绝对路径
// Mac/Linux
images: ['/Users/username/Pictures/photo.jpg']
// Windows
images: ['C:\\Users\\Username\\Pictures\\photo.jpg']

// ✅ 使用网络URL
images: ['https://example.com/photo.jpg']
```

#### 在命令中
```bash
# ✅ 通用方式（适用于任何系统）
cd xhs-mcp-service
npm start

# ✅ 使用环境变量
export XHS_PORT=18060  # Linux/Mac
$env:XHS_PORT=18060    # Windows PowerShell
set XHS_PORT=18060     # Windows CMD
```

### 4. 新增说明

#### 路径配置
- ✅ 相对路径说明（相对于项目根目录）
- ✅ 绝对路径示例（跨平台）
- ✅ 环境变量配置（端口、主机、代理）

#### 部署说明
- ✅ 开发环境部署
- ✅ 生产环境部署（PM2）
- ✅ Docker部署示例

## 📝 文档对比

### README.md
| 项目 | 之前 | 现在 |
|------|------|------|
| 项目路径 | `D:\work\...` | `xhs-mcp-service` (可自定义) |
| 命令示例 | Windows特定 | 跨平台 |
| 环境变量 | 未说明 | 详细说明 |
| 部署方式 | 仅开发 | 开发+生产+Docker |

### 示例代码
| 项目 | 之前 | 现在 |
|------|------|------|
| 路径硬编码 | ✅ 有 | ❌ 移除 |
| 相对路径 | ❌ 未说明 | ✅ 详细说明 |
| 绝对路径 | ❌ 未说明 | ✅ 跨平台示例 |
| 注释说明 | 基础 | 详细 |

## 🎯 使用指南

### 1. 克隆到任意位置
```bash
# 可以在任何位置
cd ~/projects          # Mac/Linux
cd C:\projects         # Windows
# 或其他你喜欢的位置

git clone <repo> xhs-mcp-service
cd xhs-mcp-service
```

### 2. 项目名称可自定义
```bash
# 可以使用任何名称
git clone <repo> my-xhs-bot
cd my-xhs-bot
```

### 3. 路径配置
```javascript
// 在你的脚本中
import { publishContent } from './src/xhs-tools.js';

await publishContent({
  images: [
    // 相对路径（相对于项目根目录）
    './photos/img1.jpg',

    // 或绝对路径
    '/absolute/path/img2.jpg'
  ]
});
```

## 💡 最佳实践

### 项目组织
```
your-workspace/           # 你的工作空间
├── xhs-mcp-service/      # MCP服务（名称可自定义）
├── my-photos/            # 你的图片
├── my-scripts/           # 你的脚本
└── data/                 # 数据文件
```

### 脚本位置
```bash
# ✅ 方式1: 在项目内创建脚本
xhs-mcp-service/
└── my-scripts/
    └── daily-post.js

# ✅ 方式2: 在项目外，引用相对路径
workspace/
├── xhs-mcp-service/
└── my-automation/
    └── post-bot.js       # 引用 ../xhs-mcp-service/src/xhs-tools.js
```

## 📊 改进统计

- ✅ 移除 15+ 处硬编码路径
- ✅ 添加 10+ 处跨平台示例
- ✅ 新增 3 个详细说明文档
- ✅ 更新 3 个示例代码
- ✅ 添加环境变量配置说明

## 🎊 总结

所有文档现在都：
- ✅ **通用化** - 适用于任何系统、任何位置
- ✅ **可配置** - 使用环境变量和相对路径
- ✅ **跨平台** - 提供 Windows/Mac/Linux 示例
- ✅ **易理解** - 详细的注释和说明

可以放心在任何环境中使用！🚀
