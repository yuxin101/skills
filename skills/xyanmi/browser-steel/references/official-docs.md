# Official Steel References

Use these links when the skill needs the authoritative upstream command surface or SDK guidance.

## Steel CLI

- Main overview: https://docs.steel.dev/overview/steel-cli
- Agent-friendly docs index: https://docs.steel.dev/llms-full.txt

Use the Steel CLI reference when:

- command names or flags may have drifted
- session lifecycle semantics need confirmation
- local vs cloud vs self-hosted mode needs exact upstream wording
- a new CLI subcommand should be added to the wrapper

## Playwright + Steel (Python)

- Guide: https://docs.steel.dev/overview/guides/playwright-python

Use this guide when:

- extending the Python runtime
- checking SDK + Playwright session patterns
- validating how to connect over CDP or when to release sessions explicitly

## Rule

Treat this file as an external-reference router, not a place to duplicate the upstream docs.
