# 各识别类型的字段说明（elements 内容）

根据 ocrType 不同，返回的 `elements` 对象包含以下字段：

## ID_CARD (大陆身份证)
- `name`: 姓名
- `gender`: 性别
- `nation`: 民族
- `bornDate`: 出生日期
- `address`: 住址
- `IDNumber`: 公民身份号码
- `issueInstitution`: 签发机关
- `validityPeriod`: 有效期
