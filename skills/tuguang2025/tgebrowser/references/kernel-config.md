# Browser kernel config

Used under **fingerprint** → **kernel** (version string) for create-browser / update-browser.

Only Chrome is supported. Pass the version as a string.

## Supported values

`133`, `135`, `137`, `139`, `141`, `143`, `ua_auto` (default)

## Example

`"fingerprint": { "os": "Windows", "kernel": "143" }`
