{
  "name": "openclaw-plugin-ain",
  "version": "0.2.2",
  "description": "OpenClaw plugin for AIN — provider registry, routing, and execution",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/felipematos/ain.git",
    "directory": "packages/openclaw-plugin-ain"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm run test"
  },
  "dependencies": {
    "@felipematos/ain-cli": "^0.11.0"
  },
  "peerDependencies": {
    "openclaw": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
