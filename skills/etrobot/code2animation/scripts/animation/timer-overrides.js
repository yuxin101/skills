// Timer function overrides - COMPLETELY DISABLED for now
export class TimerOverrides {
  constructor(core, typewriterHandler) {
    this.core = core;
    this.typewriterHandler = typewriterHandler;
    console.log('[Animation] Timer overrides completely disabled - all animations work normally');
  }
}