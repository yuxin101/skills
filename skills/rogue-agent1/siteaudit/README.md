# siteaudit 🔍

Comprehensive website audit — uptime, TLS certificates, security headers, and response times in one command. Zero dependencies.

## Usage
```bash
python3 siteaudit.py github.com openclaw.ai
python3 siteaudit.py --json example.com
```
